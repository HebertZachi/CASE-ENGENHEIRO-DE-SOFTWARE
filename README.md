## Endpoints

A aplicação expõe endpoints REST utilizando os métodos:

- GET
- POST
- PUT
- DELETE

Como Executar

1. Clonar o repositório
2. Configurar a connection string no `appsettings.json`
3. Executar o projeto:
